import AccountPage from "./AccountPage";

export const metadata = {
    title: "My Account",
    description: "Manage your Fiama Floral Shop account and orders.",
};

const Account = () => {
  return <AccountPage />
}

export default Account